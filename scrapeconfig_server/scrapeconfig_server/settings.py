"""
Django settings for scrapeconfig_server project.

Generated by 'django-admin startproject' using Django 1.9.7.

For more information on this file, see
https://docs.djangoproject.com/en/1.9/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/1.9/ref/settings/
"""

import os

# Build paths inside the project like this: os.path.join(BASE_DIR, ...)
BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
STATIC_ROOT = os.path.realpath(os.path.join(BASE_DIR, '../scrapeconfig_ui/dist'))
STATIC_URL = '/'
STATICFILES_STORAGE = 'whitenoise.storage.CompressedManifestStaticFilesStorage'
MEDIA_ROOT = os.path.abspath(os.path.join(BASE_DIR, '../data/projects'))


# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/1.9/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = 'x8#v=v_yen3pvul&2*-x3=td2eqvw%5!*qaf^g8vzu#gcyo+%n'

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True

ALLOWED_HOSTS = [
    '*'
]


# Application definition

INSTALLED_APPS = [
    'db_repo.apps.DbRepoConfig',
    'storage.apps.StorageConfig',
    'scrapeconfig_orm.apps.ScrapeConfigOrmConfig',
    'scrapeconfig_api.apps.ScrapeConfigApiConfig',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'whitenoise.runserver_nostatic',
    'django.contrib.staticfiles',
    'scrapeconfig_dashboard.apps.ScrapeConfigDashboardConfig' ,
]


MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'whitenoise.middleware.WhiteNoiseMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
    'scrapeconfig_orm.middleware.ORMDataStoreMiddleware',
]

ROOT_URLCONF = 'scrapeconfig_server.urls'

WSGI_APPLICATION = 'scrapeconfig_server.wsgi.application'


# Database
# https://docs.djangoproject.com/en/1.9/ref/settings/#databases

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': os.path.join(BASE_DIR, 'db.sqlite3'),
    }
}

# Password validation
# https://docs.djangoproject.com/en/1.9/ref/settings/#auth-password-validators

AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
    },
]


# Internationalization
# https://docs.djangoproject.com/en/1.9/topics/i18n/

LANGUAGE_CODE = 'en-us'

TIME_ZONE = 'UTC'

USE_I18N = True

USE_L10N = True

USE_TZ = True

PORTIA_STORAGE_BACKEND = 'storage.backends.FsStorage'

SCHEDULE_URL = 'http://localhost:6800/schedule.json'
SCRAPYD_URL= 'http://localhost:6800'

REST_FRAMEWORK = {
    'DEFAULT_AUTHENTICATION_CLASSES': (
        'scrapeconfig_server.backends.LocalAuthentication',
    ),
    'EXCEPTION_HANDLER': 'scrapeconfig_api.jsonapi.exceptions.jsonapi_exception_handler',
    'URL_FORMAT_OVERRIDE': None
}

CUSTOM = {}

CAPABILITIES = {
    'version_control': False,
    'create_projects': True,
    'delete_projects': True,
    'rename_projects': True,
    'deploy_projects': False,
    'rename_spiders': True,
    'rename_templates': True,
}

# domains that scrapeconfig will not load
BLACKLIST_URLS = set()
